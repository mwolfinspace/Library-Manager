[package]
name = "reader-launcher"
version = "1.0.0"
edition = "2021"

[dependencies]
zip = { version = "0.6", default-features = false, features = ["deflate"] }

[build-dependencies]
embed-resource = "2"
